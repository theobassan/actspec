{
  "name": "actharness-api-ergonomics-spike",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "node scripts/install-fixtures.js",
    "test": "vitest run 2>&1 | tee /tmp/api-ergonomics-result.txt; cat /tmp/api-ergonomics-result.txt",
    "test:watch": "vitest"
  },
  "dependencies": {
    "yaml": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.8"
  }
}
